## 问题 1
假设你已编译该程序，并在当前目录中有一个名为 `encrypt` 的可执行文件。同时假设你有一个内容如下的文件 `input.txt`：

```text
An example
Of the input.
```

如果命令行输入是：

```bash
./encrypt input.txt
```

会向 `stderr` 打印什么？

答案:
````spoiler
题目中给出的 `encryption.c` 代码不完整，缺少 `main` 函数以及错误处理逻辑，无法确定具体输出。
````

## 问题 2
如果命令行输入是：

```bash
./encrypt 0 input.txt
```

会向 `stderr` 打印什么？

答案:
````spoiler
题目中给出的 `encryption.c` 代码不完整，缺少 `main` 函数以及错误处理逻辑，无法确定具体输出。
````

## 问题 3
如果命令行输入是：

```bash
./encrypt 7 input.txt
```

会向 `stdout` 打印什么？

答案:
````spoiler
题目中给出的 `encryption.c` 代码不完整，缺少 `main` 函数以及完整的加密逻辑，无法确定具体输出。
````
