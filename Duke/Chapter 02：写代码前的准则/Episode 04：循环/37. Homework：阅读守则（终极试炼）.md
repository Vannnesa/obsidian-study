````quiz
什么是 lvalue？

[ ] 通过评估表达式而提升的值。

[c] 可以作为运算符左操作数的值。

[ ] 可放入方框中的数值。

[ ] 给盒子命名的东西。
````

问题2：
````quiz
什么是Range(定义域)？
[ ] 编写特定程序所需的领域知识量。
[ ] 函数可以解决的问题种类。
[c] 变量可见的代码区域。
[ ] 变量可容纳的值类型。
````
假设有这样一行代码:
````c
int a = f(x, y);
````

````quiz
以下哪项最恰当地描述了如何确定在**a** 的方框中填入什么值？
[ ] 查看f，找出其中有a = 表达式形式的语句的地方, 找出表达式的值 之后你得出的值就是a的值。
[ ] 看看f是什么函数 计算出x和y的算式 所得答案就是a的值
[ ] 为f创建一个frame，把x和y的值复制到为其参数命名的框中，当执行箭头移动到f时会逐行执行代码。当执行箭头到达类似printf语句时，它所打印的值就是a的值。
[ ] 为f创建一个frame，把x和y的值复制到为其参数命名的框中，当执行箭头移动到f时会逐行执行代码。当执行箭头到达类似return语句时，该表达式的值就是a的值。
````

````quiz
如果 C 语言没有关键字 "for"，但您想写一些自然选择 for 循环的内容，您可以用什么来代替？
[ ] return
[ ] if
[ ] break
[c] while
````
````quiz
printf和return有什么区别？
[ ] printf会退出当前函数 而return不会
[ ] printf是把数值显示给用户，而返回数值是把数值提供给其他函数进行进一步计算。
[ ] printf只对字符串起作用，而return只对整数有效
````
以下代码有错误：
````c_wrong
	int f (int x) {
		int answer = 0
		for (int i = 0; i < X; i++) {
			answer += i * i;
		}
		return answer;
--->
````

````quiz
第七行的错误是什么？
[c] missing )
[ ] missing {
[ ] I don't know.
[ ] missing }
````

在脑海里运行

````c
int main (void) {
  int a = 2;
  int b = 6;
  while (a <= b) {
    if (a % 2 == 1) {
      printf("a is %d\n", a);
    }
    else {
      printf("b is %d\n", b);
      for (int i = 0; i < b - a ; i++) {
        printf("a * i + b = %d\n", a * i + b);
      }
    }
    a++;
    b--;
  }
  return 0;
````
以下哪个输出是正确的？
A：


$$
\begin{aligned}
b \text{ is 6}\\&
a * i + b = 6\\ &
a * i + b = 8\\ &
a * i + b = 10\\ &
a * i + b = 12\\ 
b \text{ is 4}
\end{aligned}
$$

B：

$$
\begin{align}
b \text{ is 6}\\&
a * i + b = 8\\&
a * i + b = 10\\&
a * i + b = 12\\
a \text{ is 3}\\
b \text{ is 4}
\end{align}
$$

C：
$$\begin{align} \\
\text{b is 6}\\&
a * i + b = 6\\&
a * i + b = 8\\&
a * i + b = 10\\&
a * i + b = 12\\
\text{a is 3}\\
\text{b is 4}
\end{align}$$

D：
$$\begin{align} \\
\text{b is 6}\\&
a * i + b = 6\\&
a * i + b = 8\\&
a * i + b = 10\\&
a * i + b = 12\\
\text{a is 3}\\

\end{align}$$

````spoiler
B
````
 
 
  
 
```c
int anotherFunction(int a, int b) {
  int answer = 2;
  int x = 0;
  printf("In anotherFunction(%d,%d)\n",a,b);
  while (b > a) {
    printf("a is %d, b is %d\n", a, b);
    answer = answer + (b - a);
    b -= x;
    a += x / 2;
    x++;
  }
  return answer;
}

int someFunction(int x, int y) {
  int a = x + y;
  if (x < y) {
    for (int i = 0; i < x; i++) {
      printf("In the loop with i = %d, a = %d\n", i, a);
      a = a + x;
    }
  }
  else {
    y = anotherFunction(y,a+1);
  }
  return a * (y-10);
}

int main(void) {
  int x = 2;
  int b = someFunction(3,x);
  printf("b = %d\n", b);
  printf("x = %d\n", x);
  return 0;
}
```

以下哪个输出是正确的?
A:
$$\begin{align} \\&
\text{在i=0的循环中} \\
&a=5 \\&
\text{在i=1的循环中} \\
& a=7\\&
b = -63\\&
x = -2\\&
\end{align}$$
