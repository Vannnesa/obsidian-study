````quiz
## 问题 1
什么是抽象（abstraction）？

[ ] 移除程序中非必要的部分。
[c] 将某事物“做什么”与“如何做”分离开来。
[ ] 从具体例子转向更一般情况的行为。
[ ] 编写伪代码。
````

````quiz
## 问题 2
什么是字符串（string）？

[c] 一个字符序列
[ ] 一个含有奇数个 1 的位模式
[ ] 一段没有控制语句的代码片段
[ ] 影响同一个变量的一连串计算
````

````quiz
## 问题 3
以下哪一项不是由变量的类型决定的？

[ ] 在内存中存储它所需的位数。
[ ] 如何解释所存储的位。
[c] 变量的作用域（scope）。
[ ] 以上都不是。（以上全部都由类型决定。）
````

````quiz
## 问题 4
对于以下代码，假设 short 整数是 16 位，

```c
short y = 30000;
short x = y + 10000;
```

以下哪一项最好地描述了其中存在的类型？

[ ] 表达式 `y + 10000` 是 `short`，并且赋值给 `short x` 时不会发生溢出。
[ ] 表达式 `y + 10000` 是 `short`，并且赋值给 `short x` 时发生溢出。
[c] 表达式 `y + 10000` 是 `integer`，并且赋值给 `short x` 时发生溢出。
[ ] `short y` 在首次初始化时就溢出了。
````

## 问题 5
字符 `'!'` 的二进制值是什么？

答案:
````spoiler
**100001**
````

## 问题 6
32 位 `int` 能表示的最大值用十六进制表示是什么？（确保以 `"0x"` 开头，字母用大写）

答案:
````spoiler
**0x7FFFFFFF**
````

## 问题 7
以下代码的输出是什么？

```c
double d = 4.669;
printf("My number is %.2f.",d);
```

答案:
````spoiler
**My number is 4.67.**
````

````quiz
## 问题 8
对于以下代码，

```c
int x = 5;
double d1 = 11/x;
double d2 = 11/(double)x;
```

`d1 – d2` 的值是什么？

[ ] -2.2
[c] -0.2
[ ] 0
[ ] 0.2
[ ] 2.2
````

## 问题 9
对于以下使用枚举类型的代码，

```c
enum fruit_tag {
  BLUEBERRY,
  BANANA,
  PINEAPPLE,
  WATERMELON
};
typedef enum fruit_tag fruit_t;

void printFruit(fruit_t myFruit) {
  switch(myFruit) {
    case BLUEBERRY:
      printf("a blueberry");
      break;
    case BANANA:
      printf("a banana");
      break;
    case PINEAPPLE:
      printf("a pineapple");
      break;
    case WATERMELON:
      printf("a watermelon");
      break;
  }
}

void compareFruit(fruit_t fruit1, fruit_t fruit2) {
  if (fruit1 > fruit2) {
    printFruit(fruit1);
    printf(" is larger than ");
    printFruit(fruit2);
  }
  else {
    printFruit(fruit1);
    printf(" is smaller than ");
    printFruit(fruit2);
  }
}

int main(void) {
  fruit_t myFruit = BANANA;
  fruit_t otherFruit = WATERMELON;
  compareFruit(myFruit, otherFruit);
  return 0;
}
```

输出是什么？

答案:
````spoiler
**a banana is smaller than a watermelon**
````

## 问题 10
对于以下代码，

```c
enum name_tag {
  BLUEBERRY,
  BANANA,
  PINEAPPLE,
  WATERMELON
};
typedef enum name_tag name_t;

struct fruit_tag {
  name_t name;
  double size;
};
typedef struct fruit_tag fruit_t;

fruit_t getBigger(fruit_t f, double d) {
  f.size += d;
  return f;
}

int main(void) {
  fruit_t myFruit;
  myFruit.name = BANANA;
  myFruit.size = 5.2;
  myFruit = getBigger(myFruit, 3.4);
  printf("This fruit is %.2f grams.\n", myFruit.size);
  return 0;
}
```

输出是什么？

答案:
````spoiler
**This fruit is 8.60 grams.**
````





![[Pasted image 20260917165525.png]]