
## 观察引擎盖下

当你在车流中驾驶汽车时，可能不应该过多地考虑发动机在做什么--事实上，你真的不需要知道它是如何工作的就能开车。这个例子说明了编程中的一个重要概念：_抽象--_将界面（某物的作用或使用方法）与实现（某物的工作原理）分离开来。

抽象通常有多个层次。驾驶汽车时，你关心的抽象层次是方向盘使汽车转动，油门踏板使汽车加速，刹车使汽车减速。而机械师的抽象层面则是发动机各部件如何配合、制动液的液位如何、机油滤清器是否拧得足够紧。设计汽车的工程师考虑的是如何使汽车高效工作的物理原理。在每一个更深的层次上，你都可以思考一些在更高层次上并不重要，但对系统工作仍然至关重要的细节。我们可以继续降低抽象层级，直到我们开始思考原子的量子相互作用--幸运的是，你在并入州际公路时并不需要担心这个问题！

不过，有时还是应该看看 "引擎盖下"--比你通常关心的抽象层次更深入一些。至少，在加满油之前，你可能想知道这辆车是否装有柴油发动机，或者想知道你的车有机油，你应该偶尔换换机油。

同样，要写出优秀的代码，也不需要经常考虑 CPU 的内部工作原理。将变量视为存储数值的盒子是一种很好的抽象。但是，了解引擎盖下的工作原理也很重要。在首次声明变量并为其分配类型时，最好先暂停一下，考虑一下这在硬件层面的实际意义。

![[Pasted image 20260917095921.png]]

如前所述，类型既表示大小，也表示解释。上图展示了我们现在熟悉的代码及其概念表示。在本模块中，我们将增加第三列，向你展示硬件层面的底层表示。当你声明一个 int 类型的变量 x 时，你应该从概念上将其视为一个名为 x 的盒子，里面有一个值 42。但在硬件层面上，int 类型意味着你为这个变量分配了 32 位专用位，并选择将这些位解释为整数，从而产生 42 这个值。

### 十六进制

正如你所想象的那样，读写一连串 32 个 1 和 0 往好里说是乏味的，往坏里说是容易出错的。因此，许多计算机科学家选择使用一种名为十六进制的编码，用二进制写出他们正在思考的数字值。十六进制的基数是 16，这意味着它表示一个数字有 1 列、16 列、256 列，依此类推。由于十六进制数字可以有 16 个可能的数值（0-15），而我们的基 10 数字只有 10 个可能的符号（0-9），因此我们用字母 A-F 来表示一个数字中的 10-15 个数值。例如，数字 11 在十六进制中用一个数字 "B "表示。

二进制表示的数字可以很容易地转换成十六进制，只需将它们分成 4 个数字组，每个数字组都可以用一个十六进制数字来表示。例如，上图中最右边的 4 个位（蓝色）是 1010，十进制值为 10，十六进制值为 A。数字的其余 24 位均为 0。与其写出整个 32 位二进制序列，我们可以使用 8 位十六进制（0x0000002A）或速记 0x2A。(在这两种情况下，前导 0x（可与 x 互换）表示数字为十六进制）。