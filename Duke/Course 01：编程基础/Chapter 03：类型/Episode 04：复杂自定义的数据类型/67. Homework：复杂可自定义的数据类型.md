````quiz
## 问题 1
什么是 struct（结构体）？

[ ] 一种编程构造，允许程序员改变帧是如何被 con-struct-ed 的
[ ] 一种指定特殊 in-struct-ions 的编程构造
[c] 一种将变量捆绑成逻辑组的编程构造
[ ] 一种改变程序控制流的编程构造
````

````quiz
## 问题 2
`typedef` 的作用是什么？

[c] 为现有类型起另一个名字
[ ] 提供结构体的标签（tag）
[ ] 创建一个有类型的外部文件
[ ] 定义一个新类型
````

````quiz
## 问题 3
对于以下结构体定义，

```c
struct point_tag {
  double x;
  double y;
};
typedef struct point_tag point_t;
```

以下哪些正确地声明了一个结构体？（选择所有适用项。）

[ ] point_tag myPoint;
[c] point_t myPoint;
[ ] struct point_t myPoint;
[c] struct point_tag myPoint;
````
